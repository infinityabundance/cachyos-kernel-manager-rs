assumptions = [
  "The Configure-window lifecycle (m_running guard, unconditional close) is deterministic state logic over the action sequence; a NON-VM differential against the source-derived reference is the strongest witness.",
  "tools/cancel-oracle-ref re-declares the exact decisions from conf-window.cpp:549-550,688-701; its output IS the oracle's output for this surface.",
  "The candidate configure_trace model reproduces the same decisions; any divergence is a real residual this court must catch.",
  "The process-kill mechanics of closing (QProcess member destructor terminating the child) are Qt semantics; the court pins down the DECISION (the close is accepted and terminal), not the signal delivery.",
  "The corpus files are frozen inputs; the runner never rewrites them.",
]

normalizers = [
  { name = "stdout-capture", version = "1.0.0", source_hash = "tools/run-cancel-corpus.sh", input_domain = "CLI stdout + exit code per corpus file", transformation = "capture stdout verbatim into <name>.json and the exit code into <name>.exit; stderr is dropped", justification = "the trace JSON IS the observable; error texts are implementation-specific", falsifier = "a byte in the trace JSON, or the exit code, differing between oracle and candidate" },
]
