assumptions = [
  "finished_proc is deterministic logic over (done-status existence, exit code, dialog answer, globs); there is no machine component, so a NON-VM differential against the source-derived decisions is the strongest witness.",
  "tools/finish-oracle-ref re-declares the exact decisions from conf-window.cpp:378-405; its output IS the oracle's output for this surface.",
  "The candidate finished_proc model reproduces the same decisions; any divergence is a real residual this court must catch.",
  "The success-then-install-reentry case models the run_cmd_async re-entry: the install's completion re-enters finished_proc with the file already removed, producing the `process failed with exit code: 0` stderr line even on pacman success (an oracle quirk, not a candidate bug).",
  "The corpus files are frozen inputs; the runner never rewrites them.",
]

normalizers = [
  { name = "stdout-capture", version = "1.0.0", source_hash = "tools/run-finish-corpus.sh", input_domain = "CLI stdout + exit code per corpus file", transformation = "capture stdout verbatim into <name>.json and the exit code into <name>.exit; stderr is dropped", justification = "the outcome JSON IS the observable (stdout/stderr lines are fields of it); error texts are implementation-specific", falsifier = "a byte in the outcome JSON, or the exit code, differing between oracle and candidate" },
]
