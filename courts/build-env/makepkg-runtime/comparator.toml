version = "1.0.0"
fixture = "makepkg-runtime"
makepkg = true

# The VM comparison path uses the normalizers; these lists document the
# raw-evidence comparison policy.
ignore = []
byte_exact = ["commands.txt", "scf-execs.txt", "sicf-execs.txt", "aurfail-execs.txt", "makepkg-version.txt"]
json_semantic = []
volatile_prefixes = []
