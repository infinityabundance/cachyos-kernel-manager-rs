version = "1.0.0"

# Volatile artifacts excluded from comparison.
ignore = ["stdout", "stderr"]

# Serialized discovery output compared byte-for-byte after normalization.
byte_exact = ["state.json"]

# None in this court.
json_semantic = []

volatile_prefixes = ["/tmp/", "/proc/", "/run/user/"]
