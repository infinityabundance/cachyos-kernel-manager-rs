assumptions = [
  "The profile-visibility rule + flags rendering is deterministic pure logic; a NON-VM differential against the source-derived reference is the strongest witness.",
  "tools/scx-oracle-ref re-declares the exact decisions from schedext-window-internal.cpp:250-264,199-214; its output IS the oracle's output for this surface.",
  "The candidate profile_ui_visible + flags_text models reproduce the same decisions; any divergence is a real residual this court must catch.",
  "The corpus files are frozen inputs; the runner never rewrites them.",
]

normalizers = [
  { name = "stdout-capture", version = "1.0.0", source_hash = "tools/run-scx-corpus.sh", input_domain = "CLI stdout + exit code per corpus file", transformation = "capture stdout verbatim into <name>.json and the exit code into <name>.exit; stderr is dropped", justification = "the profile JSON IS the observable; error texts are implementation-specific", falsifier = "a byte in the profile JSON, or the exit code, differing between oracle and candidate" },
]
