assumptions = [
  "The flag matrix + override/fallback is deterministic pure logic over (sched, mode, config); a NON-VM differential against the source-derived reference is the strongest witness.",
  "tools/scx-oracle-ref re-declares the exact matrix from scx_loader/src/config.rs; its output IS the oracle's output for this surface.",
  "The candidate flags_for_mode model reproduces the same matrix; any divergence is a real residual this court must catch.",
  "The corpus files are frozen inputs; the runner never rewrites them.",
]

normalizers = [
  { name = "stdout-capture", version = "1.0.0", source_hash = "tools/run-scx-corpus.sh", input_domain = "CLI stdout + exit code per corpus file", transformation = "capture stdout verbatim into <name>.json and the exit code into <name>.exit; stderr is dropped", justification = "the flags JSON IS the observable; error texts are implementation-specific", falsifier = "a byte in the flags JSON, or the exit code, differing between oracle and candidate" },
]
