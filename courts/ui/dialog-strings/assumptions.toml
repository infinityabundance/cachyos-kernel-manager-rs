assumptions = [
  "The frozen oracle sources (oracle/upstream/src @ 6b4a373e; the schedext window at oracle/scx-authority/scx-manager @ f3eeaf6) are the single authority for every string; the archive is hash-verified by oracle/UPSTREAM.lock.",
  "The candidate's Slint UI must render these exact strings; this court pins the table before any rendering work, so a later UI bug cannot be blamed on the strings layer.",
  "The string table is fixed: no corpus, no fixture inputs — the witness renders the table itself.",
  "The JSON descriptor is rendered identically by both sides (serde_json pretty, fixed row order).",
]

normalizers = [
  { name = "stdout-capture", version = "1.0.0", source_hash = "tools/run-strings-corpus.sh", input_domain = "CLI stdout + exit code (fixed string table, no corpus)", transformation = "capture stdout verbatim into strings.json and the exit code into strings.exit; stderr is dropped", justification = "the descriptor JSON IS the observable", falsifier = "a byte in the descriptor, or the exit code, differing between oracle and candidate" },
]
