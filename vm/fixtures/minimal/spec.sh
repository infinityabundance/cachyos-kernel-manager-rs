#!/usr/bin/env bash
#
# minimal — the base state, untouched: linux-cachyos + headers installed from
# the cachyos repo, all sync databases as built. No fixtures repo.
#
set -euo pipefail
echo "fixture minimal: no changes"
