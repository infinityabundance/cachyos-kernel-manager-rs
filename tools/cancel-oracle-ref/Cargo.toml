[package]
name = "cancel-oracle-ref"
version = "0.1.0"
edition = "2021"
publish = false
description = "Reference implementation of the oracle's Configure-window lifecycle (on_execute m_running guard + closeEvent/on_cancel, conf-window.cpp:688-701,549-550 @ 6b4a373e) for the build-env/cancellation differential court. NOT part of the candidate; evidence infrastructure."

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[[bin]]
name = "cancel-oracle-ref"
path = "src/main.rs"
