[package]
name = "env-oracle-ref"
version = "0.1.0"
edition = "2021"
publish = false
description = "Reference implementation of the oracle's get_all_set_values env rendering (conf-window.cpp:421-451 + compile_options.json @ 6b4a373e) for the build-env/env-rendering differential court. NOT part of the candidate; evidence infrastructure."

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[[bin]]
name = "env-oracle-ref"
path = "src/main.rs"
