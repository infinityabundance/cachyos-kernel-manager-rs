[package]
name = "options-oracle-ref"
version = "0.1.0"
edition = "2021"
publish = false
description = "Reference implementation of the oracle's Configure-window variant-switch transitions (conf-window.cpp:553-602 @ 6b4a373e) for the option-transitions/variant-switch differential court. NOT part of the candidate; evidence infrastructure."

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[[bin]]
name = "options-oracle-ref"
path = "src/main.rs"
