[package]
name = "artifact-oracle-ref"
version = "0.1.0"
edition = "2021"
publish = false
description = "Reference implementation of the oracle's artifact-glob pipeline (conf-window.cpp:218-298 get_pkgext_value_from_makepkgconf + get_package_names_glob_from_pkgbuild + prepare_func_names @ 6b4a373e) for the artifact-glob/package-functions differential court. NOT part of the candidate; evidence infrastructure."

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[[bin]]
name = "artifact-oracle-ref"
path = "src/main.rs"
